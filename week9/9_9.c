#include <stdio.h>
#include <stdlib.h>
#include <string.h>


char* findLongestWord(char* str);

int main() {

  char line[100];     // 입력받을 문장 
  fgets(line, 100, stdin); 
  line[strlen(line)-1]='\0';

  // 이곳에 코드를 작성하라

  return 0;
}

char* findLongestWord(char* str){
    
}